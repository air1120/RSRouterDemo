# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'RSRouterDemo' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  pod 'JLRoutes'
  # Pods for RSRouterDemo

  target 'RSRouterDemoTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'RSRouterDemoUITests' do
    # Pods for testing
  end

end
